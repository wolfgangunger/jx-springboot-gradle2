apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/wolfgangunger/jx-springboot-gradle2:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/wolfgangunger/jx-springboot-gradle2:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: jx-springboot-gradle2
        chartPath: charts/jx-springboot-gradle2
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/wolfgangunger/jx-springboot-gradle2"
          image.tag: "{{.DIGEST_HEX}}"
